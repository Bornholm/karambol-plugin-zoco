{% macro getTags(entry) %}
  {% if entry.isClosed %}
    <span class="label label-danger">{{'plugins.zoco.tags.closed'|trans}}</span>
  {% else %}
    <span class="label label-info">{{'plugins.zoco.tags.open'|trans}}</span>
  {% endif %}
{% endmacro %}
